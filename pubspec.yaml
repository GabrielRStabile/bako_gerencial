name: bako_gerencial
description: Projeto de criação de painel gerencial para o app Aventura com Bako.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^0.22.0
  get: ^4.1.4
  google_fonts: ^2.1.0
  pixel_perfect: ^1.1.0
  simple_shadow: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/
    - assets/pixel_perfect/login_screen1.png
  uses-material-design: true
